#INPUT () FUNCTION : The input function in Python allows users to interact with a program by providing input via the keyboard. The function stops the program's execution until the user provides input. 

# Syntax:
# variable_name = data_type(input("Enter the message for user: "))

a = int(input("Enter the number: "))
print("User Entered Value is:",a) # After taking input from user here printing that value


